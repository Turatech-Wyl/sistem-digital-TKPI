# Dibaca agent (agent/) — salin ke .env bila perlu diubah
# Lokal default: DB web/prisma/dev.db, data di ../data
DB_PATH="../web/prisma/dev.db"
DATA_DIR="../data"
# Nomor untuk uji awal: npm run test-wa -- 62812xxxxxxx "halo tes"
WA_UJI=""
